"""Phoenix CLI - a lightweight, provider-agnostic AI assistant for the terminal.

Works anywhere Python 3 runs, with special attention to Termux on Android:
pure-Python dependencies, no required C compilation, and graceful handling of
terminal resizes and interrupts.
"""

__version__ = "1.0.4"
